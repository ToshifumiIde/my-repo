name: Hello # ワークフロー名
on: push # トリガー
defaults: # デフォルトシェルの指定
  run:
    shell: bash # bashシェルを指定
jobs: # ジョブの定義
  hello: # ジョブID
    runs-on: ubuntu-latest # ランナー（Ubuntuで実行）
    steps: # ステップの定義
      - run: echo "Hello World" # シェルコマンドの実行
      - uses: actions/checkout@v4 # アクションの呼び出し
