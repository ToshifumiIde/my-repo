name: JSON functions
on: push
jobs:
  dump-to-json:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${CONTEXT}"
        env:
          CONTEXT: ${{ toJSON(github) }}
          # githubコンテキストをtoJSONでダンプ
